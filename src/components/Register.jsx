import { Helmet } from "react-helmet";

const Register = () => {
  return (
    <div>
      <Helmet>
        <title>Auth Master | Register</title>
      </Helmet>
    </div>
  );
};

export default Register;